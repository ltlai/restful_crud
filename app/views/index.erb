<h1>Welcome to Notes!</h1>

<div id="add">
  <form action="/" method="post">
    <strong>Title:</strong> <input name="title"></textarea>
    <strong>Note:</strong> <textarea name="content"></textarea>
    <input type="submit" value="Add a note!">
  </form>
</div>

<% @notes.each do |note| %>
  <p>
    <b><%= note.title %></b>
    <span><a href="/<%= note.id %>/edit">[edit]</a></span>
    <span><a href="/<%= note.id %>/delete">[delete]</a></span>
  </p>
  <p>
    <%= note.content %>
  </p>
  <div>============================================</div>
<% end %>